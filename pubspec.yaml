name: ceiling_crm
description: CRM для натяжных потолков
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  intl: ^0.18.1
  share_plus: ^7.0.2
  printing: ^5.11.2
  pdf: ^3.10.7
  url_launcher: ^6.3.1
  provider: ^6.1.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/standard_positions.json
