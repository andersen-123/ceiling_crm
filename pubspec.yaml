name: ceiling_crm
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  image_picker: ^1.0.4
  
  # Для базы данных
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # Для PDF (временно закомментируем syncfusion, используем более простой вариант)
  # syncfusion_flutter_pdfviewer: ^23.2.7
  printing: ^5.11.1
  pdf: ^3.10.6
  
  # Для шаринга
  share_plus: ^7.0.0
  
  # Для работы с датами
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
